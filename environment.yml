name: camp
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.24
  - pillow>=10.0
  - pywavelets>=1.5
  - scikit-image>=0.22
  - tqdm>=4.66
  - pytorch>=2.1
  - torchvision>=0.16
  - pytorch-cuda=12.1
  - pip
  - pip:
    - blobfile
    - einops
    # - flash-attn  # 建议先注释掉，等 Torch 跑通后再手动装